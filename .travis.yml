sudo: true
dist: trusty

language: node_js
node_js:
  - "6.0"
  - "4.0"
  - "0.11"
  - "0.10"
  - "iojs"

git:
  depth: 10

env:
  - CC=clang CXX=clang++ npm_config_clang=1

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y xvfb gnome-keyring libsecret-1-0 libsecret-1-dev python-gnomekeyring

before_script:
  - sudo service dbus start

script: xvfb-run ./script/cibuild

branches:
  only:
    - master

notifications:
  email: false